# OS / editor
.DS_Store
*.swp
*~

# Local Claude Code guidance — not part of the published plugin source.
CLAUDE.md

# Local runtime cache — the plugin writes here at runtime, not part of source.
.cache/

# Node / python (we use none, but guard anyway)
node_modules/
__pycache__/
*.pyc
